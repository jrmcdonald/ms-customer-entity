plugins {
    id 'jacoco'
}

dependencies {
    api project(':customer-entity-external:customer-entity-postgresql-db')

    implementation "com.jrmcdonald:ms-common:${commonVersion}"
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-validation'
}

jar {
    enabled = true
}